name: creamy_field
description: Rich Text Editing Field & other components with rich text, selection toolbar & syntax highlight support. Useful in Rich text editors.
version: 0.3.2
homepage: https://github.com/predatorx7/snake_code/tree/master/packages/creamy_field

environment:
  sdk: ">=2.8.0 <3.0.0"
  flutter: ^1.20.0

dependencies:
  flutter:
    sdk: flutter
  flutter_highlight: ^0.6.0
  highlight: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
